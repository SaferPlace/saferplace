export type TranslationFile = {
    common: {
        email: string
        backend: string
    }
    action: {
        useEmail: string
        useBackend: string
        viewIncidents: string
    },
    phrases: {
        addToHomeScreen: string
    }
}